services:
  goframe:
    build:
      context: .
    image: goframe:latest
    environment:
      CONFIG_PATH: /config.yaml
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/config.yaml:ro
