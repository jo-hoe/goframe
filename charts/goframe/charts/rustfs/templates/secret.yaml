{{- if not .Values.credentials.existingSecret }}
{{- $secretName := printf "%s-credentials" (include "rustfs.fullname" .) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "rustfs.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
  {{- if .Values.credentials.accessKey }}
  accessKey: {{ .Values.credentials.accessKey | b64enc | quote }}
  {{- else if and $existing $existing.data $existing.data.accessKey }}
  accessKey: {{ $existing.data.accessKey | quote }}
  {{- else }}
  accessKey: {{ randAlphaNum 16 | b64enc | quote }}
  {{- end }}
  {{- if .Values.credentials.secretKey }}
  secretKey: {{ .Values.credentials.secretKey | b64enc | quote }}
  {{- else if and $existing $existing.data $existing.data.secretKey }}
  secretKey: {{ $existing.data.secretKey | quote }}
  {{- else }}
  secretKey: {{ randAlphaNum 32 | b64enc | quote }}
  {{- end }}
{{- end }}
