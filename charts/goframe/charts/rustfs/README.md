# rustfs

![Version: 1.0.0](https://img.shields.io/badge/Version-1.0.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: LATEST](https://img.shields.io/badge/AppVersion-LATEST-informational?style=flat-square)

RustFS S3-compatible object storage subchart for goframe

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| credentials | object | `{"accessKey":"","existingSecret":"","secretKey":""}` | Credentials for the built-in RustFS instance. Leave all fields empty to auto-generate a random secret. |
| credentials.accessKey | string | `""` | RustFS access key. Ignored when existingSecret is set; auto-generated when both are empty. |
| credentials.existingSecret | string | `""` | Name of a pre-existing Kubernetes Secret with keys `accessKey` and `secretKey`. Takes precedence over accessKey/secretKey below. |
| credentials.secretKey | string | `""` | RustFS secret key. Ignored when existingSecret is set; auto-generated when both are empty. |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy for the RustFS container |
| image.repository | string | `"rustfs/rustfs"` | RustFS container image repository |
| image.tag | string | `"latest"` | RustFS container image tag |
| persistence.accessMode | string | `"ReadWriteOnce"` | Access mode for the persistent volume |
| persistence.enabled | bool | `true` | Enable persistent storage for RustFS |
| persistence.size | string | `"1Gi"` | Persistent volume size |
| persistence.storageClass | string | `""` | StorageClass for the persistent volume. Empty = cluster default. |
| resources | object | `{"limits":{"cpu":"100m","memory":"128Mi"},"requests":{"cpu":"25m","memory":"64Mi"}}` | Resource limits for the RustFS container. For ~100 images, 128Mi memory is sufficient. |
| resources.limits.cpu | string | `"100m"` | CPU limit for the RustFS pod |
| resources.limits.memory | string | `"128Mi"` | Memory limit for the RustFS pod |
| resources.requests.cpu | string | `"25m"` | CPU request for the RustFS pod |
| resources.requests.memory | string | `"64Mi"` | Memory request for the RustFS pod |
| service.port | int | `9000` | Port the RustFS service listens on |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
