apiVersion: v1
kind: Service
metadata:
  name: {{ include "goframe.fullname" . }}
  labels:
    {{- include "goframe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "goframe.selectorLabels" . | nindent 4 }}